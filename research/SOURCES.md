# Sources — IDP/ADP Architect

Intake point for the books, research, and reference material that will shape the
Architect-tier skills (`architect-*`). This mirrors how the existing Reference-tier
skills (`platform-blueprint`, `cluster-lifecycle`, etc.) credit the IDP reference
architectures, vulnerability/CDE whitepapers, and state-of reports they're distilled
from.

**Status:** awaiting attachments — no sources logged yet.

## How this gets used

Once sources are attached, each is logged below with:

- Title / author
- What it informs (which `architect-*` skill or domain-model entity)
- Link or file reference

Each `architect-*` skill's SKILL.md front matter will then cite the specific sources
that shaped its logic, the same way the Reference tier does.

## Log

| Source | Informs | Link/ref |
|---|---|---|
| _pending_ | | |
