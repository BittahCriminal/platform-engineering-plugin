{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WorkItem",
  "description": "A link between a discovered platform entity and an external work-tracking ticket.",
  "type": "object",
  "required": ["external_ref", "system", "linked_entity"],
  "properties": {
    "external_ref": { "type": "string", "description": "Ticket ID/URL in the source system" },
    "system": { "type": "string", "enum": ["linear", "jira", "github", "azure-devops", "other"] },
    "linked_entity": { "type": "string", "description": "ID of the Resource/Workload/Persona/Product this ticket relates to" },
    "status": { "type": "string" }
  }
}
